# Generate .htpasswd file for HTTP Basic Authentication
# This file will contain the username and encrypted password
# 
# To generate this file, use one of these methods:
#
# Method 1: Using htpasswd command (if Apache utils installed)
#   htpasswd -c docker/nginx/.htpasswd b2badmin
#
# Method 2: Using openssl (available on most systems)
#   echo "b2badmin:$(openssl passwd -apr1 your_password)" > docker/nginx/.htpasswd
#
# Method 3: Using online generator
#   Visit: https://hostingcanada.org/htpasswd-generator/
#   Or: https://www.web2generators.com/apache-tools/htpasswd-generator
#
# Default credentials (CHANGE THESE IN PRODUCTION!):
# Username: b2badmin
# Password: changeme
#
# Example line format:
# b2badmin:$apr1$xyz12345$abcdefghijklmnopqrstuv

# PLACEHOLDER - REPLACE WITH YOUR GENERATED PASSWORD
# Run: echo "b2badmin:$(openssl passwd -apr1)" > docker/nginx/.htpasswd
b2badmin:$apr1$rBXqc4kK$F3GkNv8E6n7cN.Y9rXZu91
